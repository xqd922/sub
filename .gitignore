# Dependencies
/node_modules

# Build output
/dist
/.wrangler
*.tsbuildinfo

# Local secrets — wrangler.toml is committed (no secrets in it),
# but .dev.vars and .env hold actual secrets and must stay local.
.dev.vars
.dev.vars.*
!.dev.vars.example
.env
.env.*
!.env.example

# Editors / OS
.DS_Store
*.pem
.idea
.vscode/settings.json

# Debug logs
npm-debug.log*
bun-debug.log*

# Claude Code
.claude/settings.local.json
