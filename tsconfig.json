{
  // Root tsconfig — used only as a project-references container.
  // Actual compilation is split between worker and web by the IDE / typecheck script.
  "files": [],
  "references": [
    { "path": "./tsconfig.worker.json" },
    { "path": "./tsconfig.web.json" }
  ]
}
